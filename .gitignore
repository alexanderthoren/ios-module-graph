# Per-project output workspaces (the justfile routes all artifacts here).
out/
# Legacy root-level artifact names — still the defaults when driving
# `python3 -m modgraph` directly without explicit paths.
dependency_graph.html
index_graph.json
dependency_graph_index.html
migration_plan.md
build_times.json
build_floors.json
build_history.jsonl
.swiftstats
spike_divide.py
spike_plan.py
__pycache__
.build
.env
.claude
